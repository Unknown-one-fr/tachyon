{
  "schemaVersion": 1,
  "id": "tachyon",
  "version": "${version}",
  "name": "Tachyon",
  "description": "Experimental max-performance server engine: parallel region ticking (Mosaic), struct-of-arrays entity hot-path, FFM off-heap scratch arenas, SIMD worldgen, and a self-tuning MSPT governor. Built for JDK 25 / MC 26.1.2. PLAYGROUND BUILD — expect instability and mod incompatibility; not for production worlds.",
  "authors": ["bosst"],
  "contact": {},
  "license": "MIT",
  "environment": "server",
  "entrypoints": {
    "main": ["dev.tachyon.TachyonMod"]
  },
  "mixins": ["tachyon.mixins.json"],
  "accessWidener": "tachyon.accesswidener",
  "depends": {
    "fabricloader": ">=0.19.0",
    "minecraft": "~26.1.2",
    "java": ">=25",
    "fabric-api": "*"
  }
}
