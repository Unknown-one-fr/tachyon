accessWidener	v2	official

# MC 26.1 is unobfuscated, so access-widener entries use the `official` (native Mojang)
# namespace. Entries are added as the mixin layer needs to reach private MC internals
# (entity lists, chunk maps, light engine), e.g.:
#   accessible  field  net/minecraft/server/level/ServerLevel  entityManager  L...;
