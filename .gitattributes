# Normalize text to LF in the repo; let working copies use native EOL on checkout.
* text=auto eol=lf

# Windows launcher must stay CRLF
gradlew.bat text eol=crlf

# Binary artifacts
*.jar binary
*.png binary
*.zip binary
